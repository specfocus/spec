{
  "name": "work",
  "private": true,
  "scripts": {
    "init": "sudo npm install -g pnpm & pnpm install", 
    "build": "turbo build",
    "clean": "turbo clean",
    "dev": "turbo dev --no-cache --continue --concurrency 11",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "lint": "turbo lint",
    "test": "turbo test"
  },
  "devDependencies": {
    "prettier": "^2.7.1",
    "turbo": "latest"
  },
  "packageManager": "pnpm@7.15.0"
}
